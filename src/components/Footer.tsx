import { Container } from "@mui/material";

const Footer = () => {
  return (
    <Container
      sx={{
        backgroundColor: "red",
        position: "fixed",
        bottom: 0,
        padding: "10px",
      }}
    >
      Footer
    </Container>
  );
};

export default Footer;
